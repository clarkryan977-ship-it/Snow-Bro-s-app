{
  "name": "snowbros-app",
  "version": "1.0.0",
  "description": "Snow Bro's - Professional Residential Lawn Care & Snow Removal",
  "scripts": {
    "install:all": "cd client && npm install --legacy-peer-deps && cd ../server && npm install",
    "build": "cd client && npm install --legacy-peer-deps && npm run build",
    "start": "cd server && node index.js",
    "dev": "cd server && node index.js"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
